# Auth feature (framework)

UI for Better Auth scaffolding. No Core identity / AuthZ.

```text
features/auth/
└── components/
    ├── login-form.tsx
    ├── logout-panel.tsx
    └── unauthorized-panel.tsx
```

Server helpers live in `lib/auth/`.
