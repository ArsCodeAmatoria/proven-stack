Go worker binaries live under `go/cmd/*`.

This directory is reserved as an optional app entry mirror per the repository plan.
Use:

```bash
cd go && go run ./cmd/notify-worker
```

See [Go Workers Architecture](../../docs/architecture/GO_WORKERS_ARCHITECTURE.md).
