# OpenAPI contracts

Canonical snapshot of the Proven API OpenAPI document.

Regenerate:

```bash
just docs
# or: ./scripts/codegen/export-openapi.sh
```

Live UIs (when API is running): `/docs` (Swagger), `/redoc` (Redoc), `/api-docs/openapi.json`.
