# DDD Guidelines

- Model around bounded contexts (Core, Projects, Safety, Equipment, …).
- Keep ubiquitous language in docs and types.
- Persistence and transport adapters sit outside domain cores.
- Until domain crates exist, do not invent parallel models in web/Go.

See domain docs under [`docs/architecture/`](../architecture/).
