# Seeds & Fixtures

Executable seeds (`db/seeds/local|ci/*.sql`) remain comment-only until domain tables exist.

Demo content lives in:

- [`db/seeds/fixtures/*.json`](../../db/seeds/fixtures/) — realistic construction sample data
- [`db/seeds/templates/*.sql.template`](../../db/seeds/templates/) — future INSERT shapes

Do not run templates as SQL today.
