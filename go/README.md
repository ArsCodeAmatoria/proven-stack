# Proven Go Workers

Foundation I/O worker binaries. No domain authority.

```bash
go run ./cmd/notify-worker
go run ./cmd/temporal-io-worker
go run ./cmd/media-worker
go run ./cmd/analytics-worker
```

Health: `GET /healthz`, `GET /readyz`
